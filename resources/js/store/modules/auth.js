const state = () => ({
  user: null
});

const mutations = {

};

const actions = {

};

const getters = {
  getLoggedInState: state => state.user !== null
};

export default {
  state,
  mutations,
  actions,
  getters
};